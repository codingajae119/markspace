"""user_settings 도메인 패키지 — 사용자별 개인 설정(자동 저장 등).

s01 계약(단일 Settings·세션 인증·공통 Read 스키마 규약·``user`` FK)을 재사용하는
additive 확장이다. 세션(s02)이 확정한 현재 사용자 본인의 설정만 조회·수정하며,
``user`` 테이블을 변경하지 않고 별도 ``user_setting`` 1:1 테이블에 보관한다.
"""
