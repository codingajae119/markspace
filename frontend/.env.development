# 개발 모드 전용(Vite mode=development). 프로덕션 빌드·vitest(test)에는 로드되지 않는다.
# API 호출을 same-origin 상대경로로 만들어 vite.config.ts 의 server.proxy(/api → :8000)를 타게 한다.
# 백엔드가 모든 API 를 /api/1.0 하위에 마운트하므로 base 에 버전 prefix 까지 포함한다.
VITE_API_BASE_URL=/api/1.0
